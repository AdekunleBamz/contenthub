# Manifest Check

- Open installed-app metadata and manifest references during review.
- Confirm icons and names match the current product identity.
- Record stale manifest values before release.
- Compare manifest image URLs against the production domain.
- Confirm manifest review uses the same domain as Farcaster review.
