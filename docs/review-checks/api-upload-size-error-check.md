# API Upload Size Error Check

- Upload a file that is just over the supported size limit.
- Confirm the API returns safe, user-facing error copy.
- Verify no Pinata response details or JWT fragments are exposed.
