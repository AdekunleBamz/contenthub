# NFT Metadata Check

- Open NFT metadata after minting.
- Confirm content URI, media URI, and creator fields resolve.
- Store the metadata URI with the transaction hash in release evidence.
- Compare the metadata URI against the gallery card media.
