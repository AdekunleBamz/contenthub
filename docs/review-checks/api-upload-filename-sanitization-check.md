# API Upload Filename Sanitization Check

- Upload files with spaces, punctuation, and path-like names.
- Confirm stored filenames or metadata labels are sanitized safely.
- Verify download links do not expose local path fragments.
