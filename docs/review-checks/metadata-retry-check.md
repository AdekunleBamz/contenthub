# Metadata Retry Check

Retry a failed metadata upload and confirm the retry replaces the failed state.
Record the first error, retry trigger, and final metadata URL.
Do not reuse failed metadata URLs in release evidence.
