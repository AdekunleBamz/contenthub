# Metadata Gateway Check

Open the metadata gateway URL and confirm it resolves without private credentials.
Record the gateway host, CID prefix, and response status.
Do not store API keys in screenshots.
