# Upload Progress Retry Check

Force a failed upload and confirm retry copy appears without duplicating metadata.
Record the first error, retry action, and final upload result.
Do not reuse failed CIDs in release notes.
